# nvelden.r-universe.dev

Configuration for the [nvelden r-universe](https://nvelden.r-universe.dev):
an auto-built CRAN-like repository (including WebAssembly binaries for
Shinylive/webR) for packages listed in `packages.json`.

Packages rebuild automatically on every push to their `main` branch.
